# ─────────────────────────────────────────────────────────
# Self-Healing Playwright Agent — Environment Configuration
# ─────────────────────────────────────────────────────────
#
# Copy this file to .env, .env.dev, .env.staging, or .env.prod
# and fill in the values.
#
# Loading precedence:
#   1. .env.{env}  — environment-specific defaults
#   2. .env        — local overrides (API keys, secrets)
#
# Values in .env always take priority over .env.{env} files.

# ── Environment ──────────────────────────────────────────
# Valid values: dev | staging | prod
ENV=dev

# ── Application ──────────────────────────────────────────
# Base URL for the application under test
# Default: https://www.gigantti.fi/
BASE_URL=https://www.gigantti.fi/

# ── AI Provider ──────────────────────────────────────────
# Which AI provider to use for self-healing
# Valid values: gemini | openai
AI_PROVIDER=gemini

# Google Gemini
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-flash-latest

# OpenAI (supports comma-separated list for automatic key rotation)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_API_KEYS=key1,key2,key3
OPENAI_MODEL=gpt-4o

# ── Logging ──────────────────────────────────────────────
# Controls file log level. Valid: error | warn | info | debug
LOG_LEVEL=info

# Controls console log level independently of file log level
CONSOLE_LOG_LEVEL=info

# ── Test Configuration ───────────────────────────────────
# Global test timeout in milliseconds (default: 120000)
TEST_TIMEOUT=120000

# Run browser in headless mode (default: true)
# Set to "false" for visible browser during development
HEADLESS=true
